CC=gcc 
CFLAGS=-std=c99 -pedantic -pedantic-errors -Werror -Wall -Wextra

all: args arr_ptr arrays basics expr hello range odd sizeof strings structs switch typedef unions

basics: basics.c
expr: expr.c
hello: hello.c
range: range.c
odd: odd.c
sizeof: sizeof.c
strings: strings.c
structs: structs.c
switch: switch.c
typedef: typedef.c
unions: unions.c

clean:
	rm -f args
	rm -f arr_ptr
	rm -f arrays
	rm -f basics
	rm -f expr
	rm -f hello
	rm -f range
	rm -f odd
	rm -f sizeof
	rm -f strings
	rm -f structs
	rm -f switch
	rm -f typedef
	rm -f unions
